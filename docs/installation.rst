============
Installation
============

At the command line::

    $ easy_install kicktipper

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv kicktipper
    $ pip install kicktipper
